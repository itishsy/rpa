<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.seebon.rpa.repository.mysql.RobotInputTypeDao">

    <insert id="insertInfo">
        insert into robot_input_type(input_name,input_id,`type`) values(#{inputName},#{inputId},#{type});
    </insert>
</mapper>